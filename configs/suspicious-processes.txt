# AI-Bastion — Suspicious Process Patterns
# One pattern per line. Lines starting with # are comments.
# Used by Layer 4 (process monitor).
# These are grep patterns matched against `ps aux` output.

# Reverse shells
nc -l
ncat
socat
/dev/tcp

# Download-and-execute chains
curl.*|.*sh
wget.*|.*sh

# Suspicious encoding/decoding
base64.*decode

# Python-based reverse shells
python.*-c.*import.*socket
python3.*-c.*import.*socket

# Known tunneling tools used in attacks
bore.pub
ngrok
cloudflared.*tunnel

# Crypto miners (common in compromised systems)
xmrig
minerd
cryptonight
